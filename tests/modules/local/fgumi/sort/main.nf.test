nextflow_process {

    name "Test Process FGUMI_SORT"
    script "modules/local/fgumi/sort/main.nf"
    process "FGUMI_SORT"

    tag "modules"
    tag "modules/local"
    tag "modules/local/fgumi"
    tag "modules/local/fgumi/sort"

    test("test - stub") {
        // Stub-mode contract test: verifies coordinate-sorted BAM/index outputs.
        options "-stub"

        when {
            process {
                """
                input[0] = [
                    [id: "test"],
                    file("https://github.com/nf-cmgg/test-datasets/raw/main/data/genomics/homo_sapiens/illumina/bam/consensus.bam", checkIfExists: true)
                ]
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }
    }

}
