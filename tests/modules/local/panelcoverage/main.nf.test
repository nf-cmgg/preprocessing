nextflow_process {

    name "Test Process PANELCOVERAGE"
    script "modules/local/panelcoverage/main.nf"
    process "PANELCOVERAGE"

    test("Should run without failures") {

        when {
            params {

            }
            process {
                """
                // define inputs of the process here. Example:
                input[0] = [[id:'test', single_end:false], file(params.test_data['homo_sapiens']['illumina']['test_per_base'], checkIfExists: true)]
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
