nextflow_workflow {

    name "Test Workflow CMGGPREPROCESSING"
    script "workflows/cmgg-preprocessing.nf"
    workflow "CMGGPREPROCESSING"

    test("cmggpreprocessing flowcell") {
        config "conf/test_flowcell.config"
        when {
            params {}
            workflow {}
        }

        then {
            assert workflow.success
        }

    }

    test("cmggpreprocessing fastq") {
        config "conf/test_fastql.config"

        when {
            params {}
            workflow {}
        }

        then {
            assert workflow.success
        }

    }

    test("cmggpreprocessing cram") {
        config "conf/test_cram.config"

        when {
            params {}
            workflow {}
        }

        then {
            assert workflow.success
        }

    }

}
