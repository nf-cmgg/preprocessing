nextflow_pipeline {

    name "Test Main Workflow"
    script "main.nf"

    test("Main workflow - default") {

        when {
            params {

            }
        }

        then {
            assert workflow.success
        }

    }
}
