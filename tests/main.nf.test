nextflow_pipeline {

    name "Test Main Workflow"
    script "main.nf"

    test("Main workflow - BCL inputs") {

        when {
            params {

            }
        }

        then {
            assert workflow.success
        }

    }
}
