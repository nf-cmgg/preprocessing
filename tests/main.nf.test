nextflow_pipeline {

    name "Test Main Workflow"
    script "main.nf"

    test("Main workflow - default") {

        when {
            params {
                run_coverage = true
                disable_picard_qc = false
            }
        }

        then {
            assert workflow.success
        }

    }
}
